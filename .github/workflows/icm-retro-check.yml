name: ICM Retro Check

on:
  push:
    branches:
      - icm-codex-context-wave-2

permissions:
  contents: write

jobs:
  check:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: icm-codex-context-wave-2
      - uses: oven-sh/setup-bun@v2
      - run: bun install --frozen-lockfile
      - run: bun scripts/apply-icm-retro-mode-carve.ts
      - run: bun test test/retro-progressive-sections.test.ts
      - run: bun test test/pair-agent-progressive-sections.test.ts
      - run: bun test test/document-generate-progressive-sections.test.ts
      - run: bun test test/qa-only-progressive-sections.test.ts
      - run: bun test test/plan-tune-progressive-sections.test.ts
      - run: bun test test/devex-review-progressive-sections.test.ts
      - run: bun test test/design-review-progressive-sections.test.ts
      - run: bun test test/parity-sectioned.test.ts
      - run: bun run scripts/gen-skill-docs.ts --host codex --out-dir /tmp/gstack-wave2
      - run: wc -c /tmp/gstack-wave2/.agents/skills/gstack-retro/SKILL.md /tmp/gstack-wave2/.agents/skills/gstack-retro/sections/*.md
      - run: rm .github/workflows/icm-retro-check.yml scripts/apply-icm-retro-mode-carve.ts
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "feat(retro): route modes through progressive Codex sections"
          git push origin HEAD:icm-codex-context-wave-2
